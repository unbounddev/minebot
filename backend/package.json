{
  "name": "minebot",
  "bin": {
    "imba-project": "bin"
  },
  "version": "0.0.0",
  "scripts": {
    "test": "./bin bot1",
    "dev": "imba build --platform node -w main.imba",
    "build": "imba build --platform node main.imba",
    "sync": "npm i && npm run build && npm link",
    "prepack": "npm i && npm run build",
    "check": "npm pack --dry-run"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "canvas": "^3.2.3",
    "commander": "^15.0.0",
    "mineflayer": "^4.37.1",
    "prismarine-viewer": "^1.33.0"
  },
  "devDependencies": {
    "imba": "^2.0.0-alpha.251"
  }
}
